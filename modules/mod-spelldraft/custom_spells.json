{
  "version": 2,
  "custom_id_range": [
    200000,
    299999
  ],
  "custom_id_offset": 200000,
  "runtime_max_level": 60,
  "selection": {
    "mode": "current_spelldraft_roots",
    "max_first_rank_level": 20,
    "force_include_roots": [
      6143
    ],
    "exclude_classes": [
      "DEATHKNIGHT"
    ],
    "include_talents": false,
    "description": "Normalize every currently draftable non-talent class ability whose first rank is learned at level 20 or earlier, plus explicitly reviewed force_include_roots needed by audited package cards. The native rank family is kept as profile/anchor source data and is replaced by one deterministic custom card using custom_id = 200000 + native_root."
  },
  "pinned": [
    {
      "clone_from": 100,
      "label": "Charge without stance restriction",
      "changes": {
        "stance_mask": 0,
        "stance_exclude": 0
      }
    }
  ],
  "scaling": {
    "strategy": "profiled_native_rank_anchors",
    "interpolation": "piecewise_linear",
    "duplicate_level_rule": "strongest",
    "below_first_rank": "level_offset_ratio",
    "low_level_offset": 2.0,
    "random_range": "runtime_roll",
    "integer_amounts": "nearest_integer",
    "duration": "piecewise_linear_nearest_1s",
    "cast_time": "piecewise_linear_nearest_0.1s",
    "periodic_damage": "per_tick_integer_interpolation",
    "damage_with_dot": [
      "direct_min_max",
      "dot_tick_min_max",
      "dot_total_reference",
      "duration",
      "tick_interval",
      "cast_time"
    ],
    "notes": "Native WoW ranks are semantic anchors. Specialized profiles keep fields with their real meaning instead of treating every effect as an anonymous min/max pair. Periodic damage is interpolated as the integer per-tick value AzerothCore actually executes; the visible total is always derived from that tick amount and the runtime tick count, so server and tooltip cannot diverge through integer division. Native anchors also retain dot_total as a reference invariant and must reproduce it exactly. Missing player levels are interpolated. Generic effect curves remain as a fallback for profiles not specialized yet. Spell power, crit, haste, buffs/debuffs and target modifiers remain normal AzerothCore mechanics and are not baked into the base level curves."
  },
  "balance_overrides": {
    "686": {
      "label": "Shadow Bolt volatile nuke",
      "direct_range_spread_multiplier": 3.5,
      "notes": "Preserve average damage while making Shadow Bolt the most volatile casted nuke."
    },
    "348": {
      "label": "Immolate persistent damage",
      "periodic_multiplier": 1.1,
      "notes": "Keep initial impact unchanged and raise periodic damage slightly."
    },
    "585": {
      "label": "Smite late-game Holy lift",
      "direct_multiplier_from_level": {
        "level": 61,
        "multiplier": 1.05
      },
      "notes": "Small late-game direct-damage increase; level 60 approaches the +5% anchor through interpolation."
    }
  }
}
