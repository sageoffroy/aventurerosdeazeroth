// Aventureros de Azeroth - metaprogression module loader.
//
// The runtime logic currently lives in ALE Lua scripts.  This native loader is
// intentionally minimal, but it makes this directory a first-class AzerothCore
// module so the core discovers it, reports it in the module graph, installs its
// config file, and includes its module .cmake integration.

void Addmod_aventureros_progressionScripts()
{
}
